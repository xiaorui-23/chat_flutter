name: chat_flutter
description: 一个用用于聊天记录展示的 flutter 插件工具包。
version: 1.0.0
homepage: https://github.com/xiaorui-23/chat_flutter

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true


